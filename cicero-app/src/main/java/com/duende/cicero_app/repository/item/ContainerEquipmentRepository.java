package com.duende.cicero_app.repository.item;

import com.duende.cicero_app.model.item.ContainerEquipmentModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContainerEquipmentRepository extends JpaRepository <ContainerEquipmentModel, Long> {}
